# 压测扩展 - 自定义 pg 镜像预装 pgbench 等工具
# 用法: docker compose -f docker-compose.yaml -f docker-compose.bench.yaml up -d pg
version: "3"
services:
  pg:
    build:
      context: .
      dockerfile: Dockerfile.pg
    image: yb-bench:latest
